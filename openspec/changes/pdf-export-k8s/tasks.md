## 1. Go Backend — Port & Adapters

- [x] 1.1 Create `backend/internal/ports/pdf.go` with `PDFRenderer` interface and `ErrPDFDisabled` sentinel error
- [x] 1.2 Create `backend/internal/adapters/pdf/disabled.go` implementing `DisabledRenderer` (Null Object, returns `ErrPDFDisabled`)
- [x] 1.3 Create `backend/internal/adapters/gotenberg/client.go` implementing `GotenbergClient` (multipart POST to `/forms/chromium/convert/html`)
- [x] 1.4 Add `GOTENBERG_URL` and `PDF_RENDER_TIMEOUT` to environment config (`backend/internal/config/config.go` or equivalent)
- [x] 1.5 Wire `PDFRenderer` in `main.go`: inject `GotenbergClient` if `GOTENBERG_URL` is set, `DisabledRenderer` otherwise

## 2. Go Backend — PDF Export Endpoint

- [x] 2.1 Add `POST /api/v1/sessions/:id/export/pdf` route to the Gin router
- [x] 2.2 Implement handler: load session → generate HTML via `ports.Generator` → call `ports.PDFRenderer.RenderPDF` → stream PDF bytes with correct headers
- [x] 2.3 Handle `ErrPDFDisabled` → 501 response with JSON body `{"error": "PDF export is not available", "reason": "no PDF renderer configured"}`
- [x] 2.4 Handle renderer errors (Gotenberg unreachable/timeout) → 502 response with JSON error body
- [x] 2.5 Handle session not found → 404 response

## 3. HTML Asset Resolution for Gotenberg

- [x] 3.1 Verify that HTML generated by `ports.Generator` uses absolute asset URLs (no relative `./static/` paths that Gotenberg cannot resolve)
- [x] 3.2 If relative paths exist in generated HTML, add an `updatePathsForPDF` transformation step in the handler to rewrite them to absolute in-pod paths or inline critical CSS

## 4. Local Dev — docker-compose

- [x] 4.1 Add `gotenberg` service to `docker-compose.dev.yml` using `gotenberg/gotenberg:8`, exposing host port `3001`, with a comment explaining it is optional for PDF support
- [x] 4.2 Add commented `GOTENBERG_URL=http://localhost:3001` entry to `.env.local.example` with description

## 5. Kubernetes Infra — Gotenberg

- [x] 5.1 Create `infra/k8s/gotenberg.yaml` with `Deployment` (image: `gotenberg/gotenberg:8`, non-root user, resource requests/limits, readiness probe on `/health`)
- [x] 5.2 Add `ClusterIP` `Service` to `infra/k8s/gotenberg.yaml` exposing port `3000`
- [x] 5.3 Add `NetworkPolicy` to `infra/k8s/gotenberg.yaml` allowing ingress from pods labelled `app: cvwonder-studio` only
- [x] 5.4 Add `GOTENBERG_URL` to the studio `Deployment` env section in `infra/k8s/studio.yaml` (value: `http://gotenberg:3000`)

## 6. Cleanup — Remove Legacy PDF Vars

- [x] 6.1 Remove `CVWONDER_PDF_GENERATION_PORT` and `CVWONDER_PDF_GENERATION_ENABLED` from all docker-compose files (`docker-compose.yml`, `examples/docker-compose.yml`, `examples/docker-compose.prod.yml`)
- [x] 6.2 Remove `CVWONDER_PDF_GENERATION_PORT` and `CVWONDER_PDF_GENERATION_ENABLED` from `lib/environment.ts` (Next.js, superseded by Go backend config)
- [x] 6.3 Remove `--port` flag usage from `cvwonder` CLI invocation in the binary adapter

## 7. Validation

- [ ] 7.1 Local dev: start `docker compose up gotenberg -d`, set `GOTENBERG_URL=http://localhost:3001`, run Go backend, verify PDF download works end-to-end
- [ ] 7.2 Local dev: unset `GOTENBERG_URL`, verify studio starts cleanly, PDF export returns 501 with expected JSON body
- [ ] 7.3 K8s: apply `gotenberg.yaml`, verify Gotenberg pods reach `Running` and pass `/health` readiness probe
- [ ] 7.4 K8s: verify NetworkPolicy blocks access to Gotenberg from pods without `app: cvwonder-studio` label
- [ ] 7.5 K8s: perform end-to-end PDF export through Ingress → studio → Gotenberg and verify PDF is downloaded correctly
